{
  "description": "Two-Eyed balance: measure the last assistant message and block an output that shipped as one eye. Stop only — a subagent's final text is a return value to another agent, not an output addressed to a human, and holding it to a reader-accountability floor would corrupt structured returns.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${PLUGIN_ROOT}/hooks/balance-check.py\"",
            "statusMessage": "Checking Two-Eyed balance",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
